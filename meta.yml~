commands:
    - type: sync
      repo: 'git@github.com:VundleVim/Vundle.vim.git'
      dst: '~/.vim/bundle/Vundle.vim'
      only: f?vim

elems:

- src: ^(vimrc)$
  dst: ~/.<1>
  only: f?vim


# vim: set filetype=eruby.yaml :
